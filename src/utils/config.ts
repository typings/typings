export const PROJECT_NAME = 'typings'
export const CONFIG_FILE = `${PROJECT_NAME}.json`
export const TYPINGS_DIR = `${PROJECT_NAME}_typings`
export const DTS_MAIN_FILE = `${PROJECT_NAME}.main.d.ts`
export const DTS_BROWSER_FILE = `${PROJECT_NAME}.browser.d.ts`
